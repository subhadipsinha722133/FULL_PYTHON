from math import sqrt
num =int(input("enter a number ="))
root =sqrt (num )
print(root)
      #write a python program to input the module and use the function accordingly.
      #create a module in python with a aaditional subtractyion in complex number
      #create a pattern name arithmetic inside the package place all the modules 
     # create a new assignment use a packagde in python programming.